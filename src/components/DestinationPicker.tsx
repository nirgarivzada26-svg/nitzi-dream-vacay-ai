// Backwards-compatible alias: the picker is now the shared DestinationSelector.
export {
  DestinationSelector as DestinationPicker,
  type DestinationSelectorProps,
} from "@/components/DestinationSelector";
